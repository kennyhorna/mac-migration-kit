PHASE: Preparation (old Mac)
- [ ] Run `bin/inventory` and read the report: it lists what exists only on this machine
- [ ] Run `bin/repo-report` and decide per project: transfer, re-clone, archive or drop
- [ ] Run `bin/ssh-audit` and decide which keys to keep
- [ ] List every paid app and where its licence lives (account / key / device-bound)
