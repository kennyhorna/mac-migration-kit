PHASE: New Mac
- [ ] {{node}} Node: install the version manager and one default version, then global packages
- [ ] {{python}} Python: version manager and `pipx` apps
- [ ] {{ruby}} Ruby: version manager and the versions you actually need
- [ ] {{rust}} Rust: `rustup default stable`, then `cargo install` your tools
- [ ] {{go}} Go toolchain
- [ ] {{java}} JVM toolchain
- [ ] Restore package-registry credentials before installing private packages
