PHASE: Capture (old Mac)
- [ ] {{new}} Create a (private) dotfiles repo from `templates/dotfiles/`
- [ ] Move shell config into it and symlink it back (`bin/link --apply`), then open a new terminal and verify
- [ ] Add app configs that are plain files (editor settings, terminal config, git config, global gitignore)
- [ ] Capture macOS defaults into `macos/defaults.sh` (Dock, Finder, keyboard, trackpad, shortcuts)
- [ ] Scan the repo history for secrets before pushing it
- [ ] Push the repo and confirm it clones on another machine
