PHASE: Capture (old Mac)
- [ ] Confirm you can sign in to the password manager on another device (recovery kit/secret key available)
- [ ] Import the SSH keys you keep into the manager and enable its SSH agent
- [ ] Put shell secrets (`export *_API_KEY=...`) into the manager and replace them with a rendered template
- [ ] Store project `.env` files and package-registry credentials as documents
- [ ] Store export-file passphrases (database client, launcher backup) in the manager
