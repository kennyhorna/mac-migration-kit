PHASE: Apps
- [ ] Build your own apps from source (use the project's build script, not just the plain build command)
- [ ] Recreate any `~/Applications` symlinks, and check anything that launches them by absolute path
