PHASE: Capture (old Mac)
- [ ] {{client}} Database client: export connections **with passwords**, store the passphrase safely
- [ ] {{launcher}} Launcher: export settings to a file, store its password safely
- [ ] {{chromium}} Browsers: note profiles, which spaces/windows belong to which profile, and extensions per profile; export bookmarks as HTML
- [ ] {{jetbrains}} JetBrains: push settings sync from the old Mac, and take a local settings backup as an escape hatch
- [ ] {{screenshot}} Screenshot/window/keyboard tools: `defaults export` their preference domains into your dotfiles
- [ ] {{iterm}} Terminal: point it at a settings folder in your dotfiles (quit it first, set it from another terminal)
