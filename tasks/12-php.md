PHASE: New Mac
- [ ] Install the PHP environment tool and open it once so its CLI exists
- [ ] Install **every** PHP version your sites use, and set the same default version as the old Mac
- [ ] Restore parked folders, linked sites, per-site PHP versions and HTTPS
- [ ] Restore `php.ini` overrides per version
- [ ] Run `composer install` per project using the tool's wrapper, then open one site per project
